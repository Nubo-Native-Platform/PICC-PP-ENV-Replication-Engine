# ==============================================================================
# PICC-PP-ENV-Replication-Engine - Production / Main Profile Configuration
# ==============================================================================

server.port=${SERVER_PORT:8082}

# Optional Centralized Config Server
spring.config.import=optional:configserver:${CONFIG_SERVER_URL:http://nnp-config-service:8080}/nnp-config

# Production Logging
logging.level.com.nnp.envrep=INFO
logging.level.feign=WARN